% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mbspls_setup.R
\name{mbspls_setup}
\alias{mbspls_setup}
\title{Set up MBSPLS parameters}
\usage{
mbspls_setup(
  sparsity,
  g_factors = NULL,
  convergence_threshold = 1e-05,
  max_iterations = 1000,
  correlation_method = c("pearson", "spearman", "kendall"),
  num_components = 2,
  matrix_norm = c("fro", "1", "2", "inf"),
  print_level = 0,
  parallel_settings = NULL
)
}
\arguments{
\item{sparsity}{Numeric vector of sparsity parameters for each block.
Values should be between 1 and sqrt(number of features) in each block.}

\item{g_factors}{Matrix or numeric value specifying the type of optimization.
Default is (1/(num_blocks-1)) * (matrix of 1s - identity matrix).}

\item{convergence_threshold}{Numeric threshold for convergence. Default: 1e-5.}

\item{max_iterations}{Maximum number of iterations for the algorithm. Default: 1000.}

\item{correlation_method}{Method for correlation computation. Default: "pearson".
Other options: "spearman" or "kendall".}

\item{num_components}{Number of latent variables/components to extract. Default: 2.}

\item{matrix_norm}{Norm used for multi-block correlation. Default: "fro".
Options: "fro" (Frobenius), 1, 2, Inf.}

\item{print_level}{Integer controlling verbosity. Higher means more output.
Default: 0 (minimal output).}

\item{parallel_settings}{List of parallel configuration options. Default: NULL.}
}
\value{
A list with all parameters needed for MBSPLS analysis.
}
\description{
This function configures all parameters needed for Multi-Block Sparse PLS analysis.
It sets defaults and validates user inputs.
}
\examples{
# Setup with default parameters for 3 blocks
params <- mbspls_setup(sparsity = c(2, 2, 2))

# Custom setup
params <- mbspls_setup(
  sparsity = c(3, 2, 4),
  convergence_threshold = 1e-6,
  max_iterations = 2000,
  correlation_method = "spearman"
)
}
