% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mbspls_run.R
\name{mbspls_run}
\alias{mbspls_run}
\title{Run MBSPLS analysis}
\usage{
mbspls_run(
  data,
  params,
  cv_folds = 10,
  n_permutations = 0,
  n_bootstraps = 0,
  seed = 42,
  verbose = TRUE
)
}
\arguments{
\item{data}{A list of matrices or an mbs_data object}

\item{params}{An mbspls_params object from mbspls_setup()}

\item{cv_folds}{Number of cross-validation folds. Default: 10}

\item{n_permutations}{Number of permutations for significance testing. Default: 0}

\item{n_bootstraps}{Number of bootstrap samples for stability assessment. Default: 0}

\item{seed}{Random seed for reproducibility. Default: 42}

\item{verbose}{Logical; if TRUE, prints progress. Default: TRUE}
}
\value{
An mbspls object with the following components:
\item{weights}{Final weight vectors for each block}
\item{latent_vars}{Latent variables for each block}
\item{rho}{Correlation values or matrix norms}
\item{cv_results}{Cross-validation results}
\item{permutation_results}{Permutation test results (if requested)}
\item{bootstrap_results}{Bootstrap results (if requested)}
\item{params}{Parameters used for the analysis}
}
\description{
This is the main function for Multi-Block Sparse PLS analysis. It handles
cross-validation, permutation testing, and bootstrapping.
}
\examples{
\dontrun{
# Create synthetic multi-block data
X1 <- matrix(rnorm(100*20), 100, 20)
X2 <- matrix(rnorm(100*30), 100, 30)
data <- mbs_data(block1 = X1, block2 = X2)

# Set up parameters
params <- mbspls_setup(sparsity = c(2, 2))

# Run MBSPLS with cross-validation
result <- mbspls_run(data, params, cv_folds = 5)
}
}
